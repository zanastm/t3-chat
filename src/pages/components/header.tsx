import React from "react";
import { boolean } from "zod/lib/types";

const Header = () => {
  return (
    <div className=" inline-flex w-full">
      <div>
        <form action="" className="inline-flex"></form>
      </div>
    </div>
  );
};

export default Header;
